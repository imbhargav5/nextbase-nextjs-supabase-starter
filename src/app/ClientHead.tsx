'use client';

// _getCssText is a function that returns the CSS text for the components
// It can only be called on the client, so we use the 'use client' pragma
export default function ClientHead() {
  return <></>;
}
