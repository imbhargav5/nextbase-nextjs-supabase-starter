---
"web": minor
---

Update every dependency to the latest release that is at least three days old (matching the workspace `minimumReleaseAge` policy). Notable majors: recharts 3, react-day-picker 10, react-resizable-panels 4, lucide-react 1, vitest 5, jsdom 30, concurrently 10, and changesets 3. The shadcn `chart`, `calendar`, and `resizable` primitives were adapted to the new APIs, and the GitHub mark now ships as a local icon because lucide-react v1 dropped brand icons.
