export const metadata = {
    title: 'Auth | TeamGrid',
    description: 'Sign in or create a TeamGrid account',
};

export default function AuthLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return children;
}
