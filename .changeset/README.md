# Changesets

Use `pnpm changeset` to record a change, `pnpm changeset:version` to apply version bumps and changelog updates, and `pnpm changeset:status` to inspect pending release input.

The release workflow opens a version PR from those files and does not publish anything to npm.
